import { Flex, SectionTitle } from '@/components';

export default function Home() {
  return (
    <Flex>
      <SectionTitle size="large">기술 이력서</SectionTitle>
      <span>안녕하세요</span>
    </Flex>
  );
}
